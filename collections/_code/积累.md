---
layout: post
title: 积累
---


## 1. 柯里化

https://juejin.cn/post/6844903882208837645

```
    let curry = (fn) => {
        return _curry(fn, fn.length)
    }
    let _curry = (fn,len,...arg) => {
        return (...params) => {
            let _arg = [...arg, ...params]
            if(len > _arg.length){
                return _curry(fn, len, ..._arg)
            }else {
                return fn.apply(this, _arg)
            }
        }
    }
    let _fn = curry(function (a, b, c, d, e) {
        console.log(a, b, c, d, e)
    });
    _fn(1, 2, 3, 4, 5);   // print: 1,2,3,4,5
    _fn(1)(2)(3, 4, 5);   // print: 1,2,3,4,5
    _fn(1, 2)(3, 4)(5);   // print: 1,2,3,4,5
    _fn(1)(2)(3)(4)(5);   // print: 1,2,3,4,5

    let list = [
        {
            name: 'lucy'
        },
        {
            name: 'jack'
        }
    ]
    let prop = curry(function (key, obj) {
        console.log(obj[key]);
        
        return obj[key];
    })
    let names = list.map(prop('name'))
```
